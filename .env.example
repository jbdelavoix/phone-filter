# Optional: used by ./stack ota (curl targets the device over HTTPS).
# Wi-Fi SSID/password are in data/config.json → LittleFS, not here.

# OTA_URL=https://192.168.1.42
# OTA_HOST=192.168.1.42
# OTA_INSECURE=1
# OTA_SKIP_BUILD=0

# Optional PlatformIO overrides when using stack for build/upload/monitor:
# ENV_NAME=esp32dev
# BAUD=115200
# PORT=
